// Supabase disabled: this project no longer uses Supabase.
// These exports are kept as no-ops to avoid breaking any legacy imports.

export const supabaseAvailable = false;

export const supabase = null;
